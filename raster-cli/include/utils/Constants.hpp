#pragma once

#include <climits>

static const char COMMENT_SYMBOL = '#';

static const size_t UCHAR_BITS = CHAR_BIT * sizeof(unsigned char);

static const int PGM_PIXEL_MAX_VALLUE = 255;
static const int PGM_PIXEL_MIN_VALLUE = 0;