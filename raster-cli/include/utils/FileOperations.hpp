#pragma once
#include <cstdint>

template <typename ImageType, typename PixelType>
inline void read_from_text_file(ImageType& image, uint64_t bytes_to_read) {}

template <typename ImageType, typename PixelType>
inline void read_from_binary_file(ImageType& image, uint64_t bytes_to_read) {}